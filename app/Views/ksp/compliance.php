<?= $this->extend('layouts/admin') ?><?= $this->section('main_content') ?>
<?= view('ksp/_header', compact('version')) ?>
<div class="card border-0 shadow-sm rounded-4 mb-4"><div class="card-body p-4 d-flex flex-column flex-md-row justify-content-between gap-3"><div><h5 class="fw-bold mb-1">KSP Compliance Preview</h5><p class="text-muted mb-0">Rule hanya dijalankan dari Regulation Registry yang dipublikasikan. Ketiadaan sumber legal menghasilkan NOT_APPLICABLE, bukan klaim patuh.</p></div><?php if(has_permission('ksp.review')): ?><form method="post" action="<?= base_url('education/ksp/'.$version['uuid'].'/compliance') ?>"><?= csrf_field() ?><button class="btn btn-primary text-nowrap">Jalankan preview</button></form><?php endif ?></div></div>
<?php if($preview): ?><div class="card border-0 shadow-sm rounded-4"><div class="card-body p-4"><div class="d-flex justify-content-between align-items-center mb-3"><h5 class="fw-bold mb-0">Hasil terakhir</h5><span class="badge text-bg-<?= $preview['run']['overall_status']==='PASS'?'success':($preview['run']['overall_status']==='FAIL'?'danger':($preview['run']['overall_status']==='WARNING'?'warning':'secondary')) ?> fs-6"><?= esc($preview['run']['overall_status']) ?></span></div><p class="small text-muted">Source revision <?= esc($preview['run']['source_revision']) ?> · <?= esc($preview['run']['generated_at']) ?></p><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Rule</th><th>Kelas</th><th>Versi/sumber</th><th>Hasil</th><th>Expected / Actual</th><th>Tindakan</th></tr></thead><tbody><?php foreach($preview['results'] as $row): ?><tr><td><strong><?= esc($row['rule_code']) ?></strong><br><small><?= esc($row['severity']) ?></small></td><td><?= esc($row['rule_class']) ?></td><td><?= esc($row['regulation_version']) ?><br><small class="text-muted"><?= esc($row['source_reference']) ?></small></td><td><span class="badge text-bg-light"><?= esc($row['result_status']) ?></span></td><td><small><strong>Expected:</strong> <?= esc($row['expected']) ?><br><strong>Actual:</strong> <?= esc($row['actual']) ?></small></td><td><small><?= esc($row['suggested_action']) ?></small></td></tr><?php endforeach ?></tbody></table></div></div></div><?php else: ?><div class="text-center text-muted p-5">Belum ada preview kepatuhan.</div><?php endif ?>
<?= $this->endSection() ?>
